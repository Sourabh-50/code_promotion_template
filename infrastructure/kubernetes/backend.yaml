apiVersion: apps/v1
kind: Deployment
metadata:
  name: opspilot-backend
  namespace: default
  labels:
    app: opspilot-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: opspilot-backend
  template:
    metadata:
      labels:
        app: opspilot-backend
    spec:
      containers:
        - name: opspilot-backend
          image: {{.Values.backend_image}}
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: opspilot-backend-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: opspilot-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
